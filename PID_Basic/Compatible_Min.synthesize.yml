synthesize:
  output-dir: ./build/quartus-compatible-min
  device: 5CSEMA5F31C6
  top: cpu
  rtl:
    base-dir: ./rtl
    defines: {REG_COUNT: 16, IMEM_DEPTH: 64, DMEM_DEPTH: 512}
    files: [alu.v, decoder.v, regfile.v, imem.v, dmem.v, cpu.v]
    include-testbench: false
    extra-files: [./rtl/cpu.sdc]
  quartus: {bin: ""}
